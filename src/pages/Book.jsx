import { useState } from 'react'; import { CalendarDays, CheckCircle2, Clock3 } from 'lucide-react'; import { PageHero } from '../components/UI'
const services=['Private Lesson','TEF Preparation','TCF Preparation','DELF Preparation']; const times=['9:00 AM','10:30 AM','1:00 PM','3:00 PM','5:00 PM'];
export default function Book(){const [service,setService]=useState('Private Lesson'); const [day,setDay]=useState('18'); const [time,setTime]=useState('10:30 AM'); const [done,setDone]=useState(false); if(done)return <section className="container-site flex min-h-[70vh] items-center justify-center py-20"><div className="max-w-xl text-center"><CheckCircle2 size={44} className="mx-auto text-cobalt"/><p className="eyebrow mt-6">Booking prototype</p><h1 className="display-md mt-3">Your class request looks good.</h1><p className="mt-5 body-lg text-muted">This is a frontend-only success state. In the production site, this step can connect to scheduling, confirmation emails, and payment.</p><button onClick={()=>setDone(false)} className="mt-8 border border-ink px-5 py-3 text-xs font-semibold uppercase tracking-[.13em]">Back to booking</button></div></section>;
return <><PageHero kicker="Book a Class" title="Choose a lesson. Pick a time. Start learning." text="A simple visual booking flow designed to make scheduling feel clear and effortless."/><section className="container-site pb-24"><div className="mb-10 grid grid-cols-4 border-y border-line">{['Service','Date','Time','Details'].map((x,i)=><div key={x} className="border-r border-line py-4 last:border-r-0"><span className="block text-[10px] text-cobalt">0{i+1}</span><span className="hidden text-xs font-semibold uppercase tracking-[.1em] sm:block">{x}</span></div>)}</div><div className="grid gap-12 lg:grid-cols-12"><div className="space-y-14 lg:col-span-8"><div><p className="eyebrow mb-4">01 — Service</p><div className="grid gap-2 sm:grid-cols-2">{services.map(x=><button onClick={()=>setService(x)} key={x} className={`border p-5 text-left font-display text-2xl transition ${service===x?'border-navy bg-navy text-white':'border-line bg-white hover:border-ink'}`}>{x}</button>)}</div></div><div><p className="eyebrow mb-4">02 — Date</p><div className="border border-line bg-white p-5"><div className="mb-5 flex items-center justify-between"><span className="font-display text-2xl">August 2026</span><CalendarDays size={20}/></div><div className="grid grid-cols-7 text-center text-xs text-muted">{['M','T','W','T','F','S','S'].map((x,i)=><span key={i} className="py-2">{x}</span>)}{Array.from({length:35},(_,i)=>i<5?'':String(i-4)).map((x,i)=>x?<button key={i} onClick={()=>setDay(x)} className={`aspect-square text-sm ${day===x?'bg-navy text-white':''} ${['16','17','18','19','20','21','22','23','24'].includes(x)?'hover:bg-cream':'opacity-30'}`}>{x}</button>:<span key={i}/>)}</div></div></div><div><p className="eyebrow mb-4">03 — Time</p><div className="flex flex-wrap gap-2">{times.map(x=><button key={x} onClick={()=>setTime(x)} className={`border px-5 py-3 text-sm ${time===x?'border-ink bg-ink text-white':'border-line bg-white'}`}>{x}</button>)}</div></div><div><p className="eyebrow mb-4">04 — Details</p><div className="grid gap-x-7 sm:grid-cols-2"><input className="field" placeholder="Name"/><input className="field" placeholder="Email"/><input className="field" placeholder="WhatsApp"/><select className="field"><option>French Level</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select><textarea className="field min-h-28 sm:col-span-2" placeholder="Learning Goal"/></div></div></div><aside className="lg:col-span-4"><div className="sticky top-24 bg-navy p-7 text-white"><p className="eyebrow !text-white/55">Booking summary</p><h2 className="mt-5 font-display text-4xl">{service}</h2><div className="mt-8 space-y-4 border-y border-white/20 py-5 text-sm"><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-white/60"><CalendarDays size={15}/> Date</span><b>Aug {day}</b></div><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-white/60"><Clock3 size={15}/> Time</span><b>{time}</b></div><div className="flex justify-between"><span className="text-white/60">Session</span><b>{service==='Private Lesson'?'60 min':'75 min'}</b></div></div><div className="mt-6 flex items-end justify-between"><span className="text-sm text-white/60">Estimated fee</span><span className="font-display text-4xl">{service==='Private Lesson'?'₱1,200':'₱1,650'}</span></div><button onClick={()=>setDone(true)} className="mt-7 w-full bg-white px-5 py-4 text-xs font-semibold uppercase tracking-[.13em] text-ink">Confirm Booking</button><p className="mt-4 text-[11px] leading-5 text-white/45">Prototype only — no appointment or payment will be created.</p></div></aside></div></section></>}
